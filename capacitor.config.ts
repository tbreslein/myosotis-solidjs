import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.myosotis.app',
  appName: 'Myosotis',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
